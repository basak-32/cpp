#include<bits/stdc++.h>

using namespace std;

int main () {
    int n;
    cin >> n;

    vector<long> inventory(n);
    for (int i = 0; i < n; i++) {
        cin >> inventory[i];
    }

    int order;
    cin >> order;

    
}